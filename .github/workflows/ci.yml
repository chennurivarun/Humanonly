name: CI

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  lint-and-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder check
        run: echo "Add lint/test steps in next sprint"
